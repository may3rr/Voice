{
  "name": "voice",
  "version": "0.1.0",
  "description": "Voice - 智能语音输入法，基于 LLM 的语音转文字工具",
  "main": "dist/main/index.js",
  "author": "Voice Team",
  "license": "MIT",
  "scripts": {
    "dev": "electron-vite dev",
    "build": "electron-vite build",
    "preview": "electron-vite preview",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint . --ext .ts,.tsx",
    "typecheck": "tsc --noEmit",
    "record-audio": "bun run scripts/record-audio.ts",
    "integration-test": "bun run scripts/integration-test.ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "electron": "^31.0.0",
    "electron-vite": "^2.3.0",
    "typescript": "^5.3.0",
    "vite": "^5.4.0",
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
